# Copy to .env.local (gitignored) and fill in real values from your
# Supabase project's Settings -> API page. VITE_SUPABASE_ANON_KEY is the
# public "anon" key, safe for client-side code -- never put the
# service_role key here or anywhere in this repo.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
